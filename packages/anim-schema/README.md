# @blud/anim-schema

Framework-agnostic schemas for:

- animation editor documents
- compiled runtime graphs
- versioned export artifacts

This package is the contract shared by the editor, compiler, exporter, and runtime.
