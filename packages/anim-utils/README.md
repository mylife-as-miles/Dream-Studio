# @blud/anim-utils

Small shared helpers used across the animation packages.

Exports:

- math helpers such as `clamp`, `lerp`, and `inverseLerp`
- a tiny emitter used by the editor store
- stable id generation for editor documents
