node_modules
dist
.DS_Store
bun.lock
coverage
.env
samples
# Upstream reference checkout for the mesh-terrain port (read-only)
# Upstream reference checkouts and the one-shot codemods used to port from them.
# Read-only working material, ~300MB of it -- none of it belongs in the repo.
/vendor/
